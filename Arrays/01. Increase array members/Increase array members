function increaseArrayMembers(args) {
    var number = +args[0],
        arr = [],
        i;
        
    for (i = 0; i < number; i += 1) {
        arr[i] = i * 5;
        console.log(arr[i]);    
    }
}
